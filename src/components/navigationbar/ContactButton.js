import React from 'react';

const ContactButton = () => {
    return (
        <button className="py-2 px-4 border-b border-transparent hover:border-black transition duration-300">
            Reach Out
        </button>
    );
};

export default ContactButton;
